# Feature Plan Lite: M1.5.5 — Align desired-state, lockfile, manifest, and managed-state terms

## Goal

Align ubiquitous language for **Configured Item**, **Desired State**, **Locked Desired State**, **Lockfile**, **Manifest**, and **Managed State** across Rust APIs, CLI help, tests, and domain docs per `CONTEXT.md`. Remove user-facing "generated state", "cache", and "internal state" wording where glossary terms apply.

## Out of scope

- M1.5.6+ (Unmanaged Artifact, Stale Discovery Requirement, Stale Installed Artifact status/prune diagnostics)
- Implementing lockfile/manifest/desired-state models (M4/M5/M6) — doc and API vocabulary only
- Renaming future milestone `desired target artifacts`, `plan records`, `resource kind` in not-yet-implemented sections unless clearly user-facing today in completed/current sections
- Introducing generic `ConfiguredItem` traits, enums with multiple kinds, or resource manager abstractions — glossary anchor + prose only; keep V1 code skill-specific
- Mass-renaming `resource_kind` in future manifest schema tasks

## Decision: Rust renames

**Rename `GeneratedStatePaths` → `ManagedStatePaths`** (and impl block). Rationale: glossary uses **Managed State**; `_Avoid_: generated state, cache, internal state`.

| Old | New | Notes |
|-----|-----|-------|
| `GeneratedStatePaths` | `ManagedStatePaths` | paths for manifest + Managed Skill Content under Managed State |
| `managed_skill_content_paths_for_project_and_user` test | `managed_state_paths_for_project_and_user` | include `managed_state` in name |

**Glossary anchor module** — add `crates/agentcfg-core/src/desired_state.rs` (or extend `config_paths.rs` with a `#[cfg(test)]` glossary section):

| Anchor | Purpose |
|--------|---------|
| `ConfiguredItemKind::Skill` + doc on **Configured Item** | V1 has one kind; skill code stays skill-specific |
| `desired_state_glossary_*` test | Documents Desired State vs Locked Desired State |
| `lockfile_glossary_*` test | Lockfile records Locked Desired State for Configured Items |
| `manifest_glossary_*` test | Manifest owns Installed Artifacts + Discovery Requirements |

Export `ConfiguredItemKind` from `lib.rs` if useful for docs; no behavior change.

Update `config_paths.rs` module doc: lockfiles, **Managed State** paths (manifest, Managed Skill Content root).

## Implementation steps

### 1. Core (`crates/agentcfg-core`)

**`config_paths.rs`**

- Module doc: "generated state" → **Managed State**
- Rename `GeneratedStatePaths` → `ManagedStatePaths`; update struct doc to reference Managed State, Manifest, Managed Skill Content
- Rename test `managed_skill_content_paths_for_project_and_user` → `managed_state_paths_resolve_manifest_and_managed_skill_content`
- Add/rename tests so `cargo test lockfile` and `cargo test manifest` match existing lockfile/manifest path tests (already present — ensure names remain)

**`desired_state.rs`** (new)

- Module doc: **Desired State**, **Locked Desired State**, relationship to preview/apply/planner
- `ConfiguredItemKind` enum with `Skill` only + glossary doc comment
- `#[cfg(test)]` tests: `desired_state_glossary_documents_outcome_before_resolution`, `lockfile_glossary_records_locked_desired_state_for_configured_items`, `manifest_glossary_records_installed_artifacts_and_discovery_requirements`
- Register `mod desired_state;` in `lib.rs`

**`workflow.rs`**

- Expand `SourceResolutionPolicy` doc: **Desired State** → **Locked Desired State** via lockfile / Source Refresh
- Add brief module-level note that preview/apply orchestrate resolution of **Locked Desired State** into **Managed State** and Client Discovery Locations (stub today)

### 2. CLI (`crates/agentcfg-cli`)

- `args.rs`: root `about` may keep "repeatable desired state" (glossary OK); ensure preview/apply `long_help` mentions **Locked Desired State** and lockfiles consistently
- `apply` about: "locked resolved state" → **Locked Desired State**
- No command renames

### 3. Documentation

| File | Changes |
|------|---------|
| `docs/prd.md` | Add **Configured Item** / **Desired State** / **Locked Desired State** / **Lockfile** / **Manifest** / **Managed State** to Terms (concise); `preview` bullet: "caches" → managed state / Managed Skill Content; `apply`: "locked resolved state" → **Locked Desired State**; lockfile bullets: record Locked Desired State for Configured Items; manifest as ownership for Installed Artifacts + Discovery Requirements |
| `docs/design-v1.md` | "generated state" → Managed State (lines ~235–236, ~293); lockfile section: records **Locked Desired State** for Configured Items; manifest section: ownership state for Installed Artifacts and Discovery Requirements (not "local generated state"); Managed Skill Content cleanup: "rebuildable cache" → rebuildable **Managed State** / unused Managed Skill Content wording; implementation boundary: "resource-specific desired state" → Configured Item / Desired State where user-facing; keep `kind = "skill"` as V1 configured item kind |
| `docs/implementation-plan-v1.md` | Check off M1.5.5; M1.2 bullet "generated state path" → managed state path resolution |
| `README.md` | "locked desired state" → **Locked Desired State** where defining apply; mention Managed State where describing `.agentcfg/` state |

**Configured Item**: Use in Terms and design prose; do not rename skill modules to `configured_item_*`.

### 4. Validation

```sh
cargo test --workspace desired_state lockfile manifest
cargo test --workspace
rg 'GeneratedStatePaths|generated state' crates/ docs/prd.md docs/design-v1.md README.md
rg -i 'cache leftover|internal state' docs/prd.md docs/design-v1.md README.md
```

Confirm no `GeneratedStatePaths` in Rust.

## Acceptance criteria

- [ ] `ManagedStatePaths` replaces `GeneratedStatePaths`; module docs use Managed State
- [ ] Glossary tests exist for `desired_state`, `lockfile`, `manifest` filters
- [ ] `ConfiguredItemKind::Skill` documents V1 single Configured Item kind
- [ ] Docs align Desired State, Locked Desired State, Lockfile, Manifest, Managed State per glossary
- [ ] `cargo test --workspace` passes; validation filters succeed
- [ ] M1.5.5 checkboxes in `docs/implementation-plan-v1.md` checked
- [ ] No user-facing "generated state" in updated docs; avoid "cache" for agentcfg-owned state in prd/design/README

## Risks

- Low: mechanical rename + doc churn
- Do not implement M1.5.6 status terms in this task
- Future milestones may still say "resource kind" until M1.5.7

## Git / PR

- Branch: `cursor/align-desired-state-terms-e922`
- Base: `main`
- Commit message: M1.5.5: Align desired state, lockfile, manifest, and managed state terms
