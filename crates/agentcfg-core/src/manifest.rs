//! **Manifest** — ownership state for **Installed Artifacts** and their
//! **Discovery Requirements** under **Managed State**.
//!
//! JSON schema and mutation are implemented in later milestones (implementation plan M6).

#[cfg(test)]
mod tests {
    #[test]
    fn manifest_glossary_term_is_module_anchor() {
        const _: &str = "Manifest owns Installed Artifacts and Discovery Requirements";
    }

    #[test]
    fn discovery_requirement_glossary_term_documented() {
        const _: &str = "Discovery Requirement keys Config Layer, Client, Install Level";
    }
}
